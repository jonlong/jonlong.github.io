{
  "name": "blog",
  "description": "Fer writin'",
  "author": "Jon Long",
  "private": true,
  "dependencies": {
    "metalsmith-drafts": "0.0.1",
    "metalsmith-templates": "~0.5.0",
    "swig": "~1.3.2",
    "metalsmith-markdown": "~0.2.1",
    "metalsmith-permalinks": "~0.3.0",
    "metalsmith": "~0.8.0",
    "metalsmith-collections": "~0.4.0",
    "metalsmith-sass": "~0.1.1",
    "gulp-sass": "~0.7.1",
    "gulp": "~3.7.0",
    "express": "~3.4.8"
  },
  "scripts": {
    "build": "node ./build",
    "start": "node ./build && node ./index",
    "deploy": "node ./build && cd ./build && git init . && git add . && git commit -m 'Deploy'; git push 'git@github.com:jonlong/writing.git' master:gh-pages --force && rm -rf .git"
  },
  "engines": {
    "node": "0.10.x"
  }
}
